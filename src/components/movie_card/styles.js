import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    backgroundColor: 'red',
    height: 300,
    borderColor: 'black',
    borderWidth: 2,
  },
});
